# Power BI file

Save the finished report here as **`ProjectManagement.pbix`**.

The report is built from the CSVs in [`../dataset/`](../dataset/) following the steps in the [dashboard README](../README.md#build-it-in-power-bi-desktop). The target layout is [`../screenshots/report.webp`](../screenshots/report.webp).

When the report is published to the Power BI service with **Publish to web**, add the public link to the dashboard README and to the portfolio (`liveUrl` in `src/data/analytics.ts`).
